<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.relaxinn">
    xmlns:tools="http://schemas.android.com/tools"

    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:replace="label">
        <activity android:name=".activitys.DisponibiliteActivity" />

        <activity android:name=".activitys.InscriptionActivity"></activity>
        <activity android:name=".activitys.ProfilActivity"></activity>
        <activity android:name=".activitys.LoginActivity"></activity>
        <activity android:name=".activitys.ListClientActivity"></activity>
        <activity android:name=".activitys.InfoActivity"></activity>

        <activity android:name=".activitys.RoomActivity" />
        <activity android:name=".activitys.ChambresCollectionActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>